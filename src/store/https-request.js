// import { createAsyncThunk } from "@reduxjs/toolkit";
// import { fetchCart } from "./cart-slice";

// export const fetchAll = createAsyncThunk("home/fetch", async (_, thunkAPI) => {
//   const res = await Promise.all([thunkAPI.dispatch(fetchCart)]);

//   console.log(res);

//   return [];
// });
